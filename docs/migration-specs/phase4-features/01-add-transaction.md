# Phase 4, Spec 1: Port Transaction Form & Add Screen

**Goal:** Implement the complex transaction creation logic natively.

**Files to Touch:**
- Create: `app/add.tsx`
- Port/Create: `src/components/Forms/TransactionForm.tsx`
- Port/Create: `src/components/Forms/TransactionTypeModal.tsx`

**Requirements:**
- Handle different transaction types (income, expense, etc.).
- Port recurrence logic (daily, monthly, indefinite).
- Integrated with Native `DateTimePicker`.
- Use `useAppTheme` for modal and button styling.

**Commit:**
- `feat: port transaction form and add screen to native`
