# Phase 4, Spec 4: Port Tag Editor Modal

**Goal:** Complete the native tag management experience.

**Files to Touch:**

- Port/Modify: `src/components/Forms/TagEditorModal.tsx`

**Requirements:**

- Implement as a Native `Modal`.
- Support color selection and calculation toggles.
- Verify `useSQL` integration for tag updates.

**Commit:**

- `feat: port tag editor modal to native`
