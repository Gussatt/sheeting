# Phase 4, Spec 2: Port Horizonte (Projection) Screen

**Goal:** Port the multi-month matrix view for financial predictability.

**Files to Touch:**

- Create: `app/horizonte.tsx`
- Reference: `src/pages/Horizonte.tsx`, `src/utils/projection.ts`

**Requirements:**

- High-fidelity grid layout using `ScrollView` (horizontal and vertical).
- Maintain existing projection logic (don't break the math).
- Port "HorizonteCell" and context-driven colors.

**Commit:**

- `feat: port horizonte projection screen to native`
